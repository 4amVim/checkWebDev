{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "chrome",
            "file": "${workspaceFolder}/index.html"
        },
                {
            "type": "firefox",
            "request": "launch",
            "name": "firefox",
            "file": "${workspaceFolder}/index.html"
        }

    ]
}